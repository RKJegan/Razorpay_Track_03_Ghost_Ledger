[server]
address = "0.0.0.0"
port = 8501
headless = true
# Required so the app works behind the sandbox preview proxy.
enableCORS = false
enableXsrfProtection = false
enableWebsocketCompression = false

[browser]
gatherUsageStats = false

[theme]
base = "dark"
